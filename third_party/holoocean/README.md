# third_party/holoocean

This directory is the reserved landing zone for HoloOcean runtime components
such as `client/` and `engine/` during the later migration stages.

No existing HoloOcean files have been moved yet.
