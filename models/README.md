# models

Repository-local storage for LLM and RL model artifacts.

Current RL checkpoints copied from the legacy framework:

- `models/rl/navigation/sac/task1-v0_SAC_3/`
- `models/rl/obstacle_avoidance/sac/task2-v0_SAC_1/`

The new framework now uses these paths as the default runtime model locations.
